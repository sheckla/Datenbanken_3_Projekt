Falls die Library 'jdbc6' in den 'External Libraries' des Projektes nicht schon vorhanden ist, 
sollte diese noch hinzugefügt werden.

Diese Library ist in dieser Ordnerstruktur unter 'external_libraries' zu finden.